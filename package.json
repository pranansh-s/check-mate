{
  "name": "check-mate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web-dev": "npm -C ./web run dev",
    "server-dev": "npm -C ./server run dev",
    "build": " npm -C ./web run build && npm -C ./shared run build && npm -C ./server run build",
    "lint": " npm -C ./web run lint && npm -C ./server run lint"
  },
  "workspaces": [
    "web",
    "server",
    "shared/"
  ]
}
